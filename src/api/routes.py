"""
FastAPI route handlers.

Defines the HTTP API endpoints for the music discovery service, including
POST /recommend for submitting discovery requests and GET /track/{id} for
fetching enriched track details. Delegates business logic to the agent layer.
"""
