"""
MCP server.

Implements an MCP-compatible server that exposes music discovery tools
(search, recommend, analyze) so that MCP-enabled clients (e.g., Claude Desktop)
can invoke the agent's capabilities directly.
"""
